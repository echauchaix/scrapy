# scrapy lengow flows

Steps to follow :
1) Go to  config_example.py and follow the instructions
2) Launch the command "scrapy crawl lengow -o export.jl" in the terminal

Then, you should get a json lines file with the last 10 catalog import logs for all the active flows
